import { PagenationPipe } from './pagenation.pipe';

describe('PagenationPipe', () => {
  it('create an instance', () => {
    const pipe = new PagenationPipe();
    expect(pipe).toBeTruthy();
  });
});
